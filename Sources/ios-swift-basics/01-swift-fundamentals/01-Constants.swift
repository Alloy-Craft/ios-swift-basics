struct Constants {

    static func run() {
        // Using "let"
        let name: String = "Alloy Craft"
        let age: Int = 22
        let isStudent: Bool = true
        
        print("My name is \(name), I'm \(age) years old and I'm a \(isStudent ? "student" : "not a student").")
    }
}
